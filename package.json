{
  "name": "cems-frontend-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix cems-frontend run dev",
    "build": "npm --prefix cems-frontend run build",
    "preview": "npm --prefix cems-frontend run preview",
    "lint": "npm --prefix cems-frontend run lint",
    "type-check": "npm --prefix cems-frontend run type-check",
    "install-app": "npm --prefix cems-frontend install"
  },
  "workspaces": [
    "cems-frontend"
  ]
}
